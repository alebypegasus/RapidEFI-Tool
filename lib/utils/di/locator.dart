import 'package:get_it/get_it.dart';

final locator = GetIt.instance;

void setupLocator() {
  // locator.registerLazySingleton<ConfigService>(() => ConfigService());
  // We will register services here as we refactor them.
}
